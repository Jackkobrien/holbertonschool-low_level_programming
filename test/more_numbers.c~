#include "holberton.h"

void more_numbers(void)
{
	char i;
	char j;
	for (j = '0'; j <= '1'; j++)
		for (i = '0'; i <= '9'; i++)
		{
			if (j = '1')
				_putchar(j);
			_putchar(i);
			if (j = '1' && i > '5')
				break;
		}

	_putchar('\n');
}
